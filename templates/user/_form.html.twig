<div class="container">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success">{{ button_label|default('save'|trans({}, 'translate')) }}</button>
    {{ form_end(form) }}
</div>